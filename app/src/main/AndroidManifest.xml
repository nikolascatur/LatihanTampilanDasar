<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comholic.latihantampilandasar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" android:label="Login" android:theme="@style/AppTheme" />
        <activity android:name=".ShowTextActivity"/>
        <activity android:name=".MainFinanceActivity" android:label="MainFinance"/>
        <activity android:name=".view.ExpensesActivity" android:label="ExpensesActivity"/>
        <activity android:name=".view.IncomeActivity" android:label="IncomeActivity"/>
        <activity android:name=".view.AddCategoryActivity" android:label="AddCategoryActivity"/>
    </application>

</manifest>